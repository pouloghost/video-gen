- 每一幕都在做animatic的关键帧，用好模型图生图做一致性，用差一点的模型做补间动画，不让视频模型做复杂的事情 ✅
- 最开始只做故事拆解和storyboard，不在开始生成角色和场景，生成到具体场景时增量生成人物和场景（资产）✅
- animatic的每一个关键帧都输入上一个关键帧作为ref ✅
- 枚举出模型常见错误 ✅
- 先做sketch再做modify，sketch的情景感和物理准确性明显好。 ✅
- 用agentic的方式获取信息：参考图、前后的panel等。可能很贵。
- 增加一个基于keyframe expand panel的过程。✅
------

split-text -> story-concept -> storyboard -> extract-refs -> character-ref -> setting-ref -> scene-frames -> tween -> merge